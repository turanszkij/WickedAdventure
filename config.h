#pragma once
#include "FontAwesomeV6.h"

namespace config
{
	static constexpr const char* const main_script_file = "adventure.lua"; // name of the script file near the exe to load at start
	static constexpr wi::Color backlog_color = wi::Color(168, 77, 94, 255); // color of backlog font (startup text)
	static constexpr wi::Color background_color = wi::Color(1, 9, 35, 255); // color of backlog background
}
